function somar (a, b) {
    return a + b
}

const operacao = somar // sem os () estou dizendo que a function vai ser "operacao"


// função anonima seria bassicamente criar ela dentro de uma variavel neste caso sem a necessidade de um nome na propria função

const subtrair = function(a, b) {
    return a - b
}